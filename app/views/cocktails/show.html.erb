
<h2><%= @cocktail.name.capitalize %> </h2>
<ul>
<% @cocktail.doses.each do |dose| %>
  <li>
    <%= dose.ingredient.name.capitalize %> ( <%= dose.description %> )
      <%= link_to "🗑", dose_path(dose), method: :delete %>

  </li>
<% end %>
</ul>


<div class="btn btn-default"> <%= link_to "Back to list", cocktails_path  %> </div>
<div class="btn btn-default"> <%= link_to "Add Ingredient", new_cocktail_dose_path(@cocktail)  %> </div>
